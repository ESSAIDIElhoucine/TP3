%{
	#include<stdio.h>
	#include<string.h>
	#include "tp3.tab.h"
%}

%option noyywrap

entier ("+"|"-")?[0-9]*
reel ("+"|"-")?[0-9]+"."[0-9]+
id [a-zA-Z][a-zA-Z0-9_]*
arith ("*"|"/"|"-"|"+")
%%
"Debut" return DEBUT;
"Fin" return FIN;
":=" return AF;
";" return TO;
{arith} return ARITH;
{entier} return entier;
{reel} return REEL;
{id} return ID;


. printf("Erreur lexicale (%s) \n",yytext);

%%